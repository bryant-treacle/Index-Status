output {
  if "elastic-health" in [tags] {
#    stdout { codec => rubydebug }
    elasticsearch {
      hosts => elasticsearch
      index => "elastic-health-%{+YYYY.MM.dd}"
#      template_name => "logstash"
#      template => "/logstash-template.json"
#      template_overwrite => true
    }
  }
}

